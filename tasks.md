# FitMatch AI-Agent-Executable Tasks

Derived from `BUILD_PLAN.txt`.

This list includes tasks an AI agent can execute end-to-end inside the product/codebase without requiring human approvals, external relationship management, or manual business operations.

## Development Phases (AI-Executable Implementation Tasks)
- [ ] Phase 1: Deliver platform foundation by scaffolding `Next.js 14` + `FastAPI`, defining PostgreSQL schemas (users/resumes/jobs/matches/notifications/watchlists/applications), adding `pgvector` migrations, defining Redis caching/freshness/rate-limit key strategy, and wiring environment templates including `OPENAI_API_KEY`.
- [ ] Phase 2: Deliver authentication and onboarding by integrating Supabase auth (magic link + OAuth), implementing onboarding capture (resume upload, role/location/work preferences), enforcing free-tier constraints, and returning a first-match response immediately after onboarding.
- [ ] Phase 3: Deliver resume intelligence core by implementing secure PDF/DOCX ingestion, OpenAI-powered parsing with `gpt-4o-mini` (contact/experience/education/skills/certifications/seniority inference), normalized resume entity persistence, and plan-based resume versioning controls.
- [ ] Phase 4: Deliver job ingestion pipeline by implementing company seed intake, scheduled crawl orchestration, ATS detection (Greenhouse/Lever/Workday/iCIMS/SmartRecruiters/BambooHR/Ashby), ATS + static + Playwright parser paths, deterministic dedup hashing, taxonomy normalization, raw snapshot + structured job persistence, and optional API-source connectors (Remotive/The Muse/USAJobs/Arbeitnow).
- [ ] Phase 5: Deliver matching engine by generating resume/job embeddings via OpenAI `text-embedding-3-small`, storing vectors in `pgvector` with similarity indexes, applying locality hard filters, computing deterministic weighted scoring (title/skills/experience/education), mapping title/seniority taxonomy, and returning ranked top-N results with normalized 0–100 scores.
- [ ] Phase 6: Deliver paid intelligence and monetization by implementing job-vs-resume skill-gap detection, fit narratives + resume rewrite guidance via OpenAI (`gpt-4o-mini`), paid resume scoring with OpenAI-assisted evaluation, plan/feature-flag model (Free/Pro/Lifetime), Stripe checkout + webhook lifecycle, tier-based gating, and paywall/pricing UX triggers.
- [ ] Phase 7: Deliver engagement and growth features by implementing notifications (preferences, triggers, email/in-app/push/SMS hooks, free digests, paid real-time alerts), company watchlists, application tracker, affiliate course recommendation pipeline (catalog ingest/cache/contextual cards/click tracking), salary benchmarking, referral rules, and API-access gating for eligible tiers.
- [ ] Phase 8: Deliver acquisition, reliability, security, and release automation by implementing programmatic SEO + auto-generated landing/FAQ/content drafts, KPI/event tracking + dashboards + operational alerts, encrypted resume storage + deletion workflows + privacy-safe analytics defaults + abuse rate limits, and full QA/CI/CD automation (unit/integration/regression tests, lint/test/migration checks, frontend/API/worker deployment pipelines).
